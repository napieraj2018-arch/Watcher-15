name: Fly exact-family diagnostic

on:
  workflow_dispatch:
  push:
    paths:
      - "src/validate_fly_exact.py"
      - "src/inspect_fly_party_html.py"
      - ".github/workflows/fly-exact.yml"

permissions:
  contents: read
  issues: write

concurrency:
  group: fly-exact-family
  cancel-in-progress: true

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 9
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements.txt
      - name: Run Fly exact family
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PYTHONUNBUFFERED: "1"
        run: |
          set +e
          python src/validate_fly_exact.py > fly.log 2>&1
          rc=$?
          {
            echo "# Fly exact-family diagnostic"
            echo
            echo "- commit: $GITHUB_SHA"
            echo "- run: $GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"
            echo "- exit_code: $rc"
            echo
            echo '```text'
            tail -n 420 fly.log
            echo '```'
          } > report.md
          gh issue edit 8 --repo "$GITHUB_REPOSITORY" --body-file report.md >/dev/null 2>&1 || true
          cat fly.log
          exit "$rc"
