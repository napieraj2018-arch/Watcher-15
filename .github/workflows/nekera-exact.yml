name: Nekera exact-family diagnostic

on:
  workflow_dispatch:
  push:
    paths:
      - "src/validate_nekera_exact.py"
      - "src/inspect_secondary_js.py"
      - ".github/workflows/nekera-exact.yml"

permissions:
  contents: read
  issues: write

concurrency:
  group: nekera-exact-family
  cancel-in-progress: true

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 9
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements.txt
      - name: Run Nekera exact family
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          CHANNEL_ID: nekera_pl
          PYTHONUNBUFFERED: "1"
        run: |
          set +e
          python src/validate_nekera_exact.py > nekera.log 2>&1
          rc=$?
          {
            echo "# Nekera exact-family diagnostic"
            echo
            echo "- commit: $GITHUB_SHA"
            echo "- run: $GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"
            echo "- exit_code: $rc"
            echo
            echo '```text'
            tail -n 360 nekera.log
            echo '```'
          } > report.md
          gh issue edit 6 --repo "$GITHUB_REPOSITORY" --body-file report.md >/dev/null 2>&1 || true
          cat nekera.log
          exit "$rc"
